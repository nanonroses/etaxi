# Database Configuration (PostgreSQL)
# Proveedores recomendados:
# - Neon: https://neon.tech/ (Serverless Postgres, recomendado para Cloudflare)
# - Supabase: https://supabase.com/database (Postgres + extras)
# - Railway: https://railway.app/ (Multi-database)
# - Vercel Postgres: https://vercel.com/docs/storage/vercel-postgres (solo con Vercel)
DATABASE_URL="postgresql://user:password@localhost:5432/etaxi?schema=public"

# NextAuth Configuration (Backoffice)
# Generate: openssl rand -base64 32
AUTH_SECRET="your_super_secret_key_here_generate_with_openssl"

# Driver API JWT Authentication (Mobile App)
# IMPORTANTE: Usar una clave DIFERENTE a AUTH_SECRET
# Generate: openssl rand -base64 32
DRIVER_JWT_SECRET="your_driver_jwt_secret_here_different_from_auth_secret"

# Passenger API JWT Authentication (Mobile App Pasajero)
# IMPORTANTE: Usar una clave DIFERENTE a AUTH_SECRET y DRIVER_JWT_SECRET
# Generate: openssl rand -base64 32
PASSENGER_JWT_SECRET="your_passenger_jwt_secret_here_different_from_others"

# Admin User (para crear el primer usuario)
ADMIN_EMAIL="admin@etaxi.cl"
ADMIN_PASSWORD="change_this_password"

# Sanity Configuration
# Para obtener estos valores:
# 1. Crea un proyecto en https://www.sanity.io/manage
# 2. Copia el Project ID de tu dashboard
# 3. El dataset por defecto es 'production'

NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Security Headers Configuration
# Allowed origin for CORS (en producción usar dominio real)
NEXT_PUBLIC_ALLOWED_ORIGIN="https://etaxi.cl"

# Application URLs
# URL base de la aplicación (para NextAuth y enlaces)
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_URL="https://www.etaxi.cl"

# Analytics & Marketing
# Google Analytics 4 - Obtener de https://analytics.google.com/
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# Meta Pixel (Facebook) - Obtener de https://business.facebook.com/
NEXT_PUBLIC_META_PIXEL_ID="XXXXXXXXXXXXXXX"

# Google Tag Manager (Opcional)
NEXT_PUBLIC_GTM_ID="GTM-XXXXXXX"

# Google Search Console Verification
# Obtener de https://search.google.com/search-console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION="your_verification_code_here"

# Environment
NODE_ENV=development

# ====================================
# INSTRUCCIONES DE CONFIGURACIÓN
# ====================================

# 1. Copia este archivo a .env.local:
#    cp .env.local.example .env.local

# 2. Genera secrets seguros:
#    openssl rand -base64 32

# 3. Configura tu base de datos:
#    - Usa Neon, Supabase, Railway u otro proveedor PostgreSQL
#    - Copia el DATABASE_URL de tu proveedor

# 4. Ejecuta las migraciones de Prisma:
#    npx prisma migrate dev
#    npx prisma generate

# 5. Crea el usuario admin inicial:
#    npx tsx scripts/create-admin.ts

# 6. (Opcional) Configura Sanity CMS:
#    - Crea proyecto en https://sanity.io/manage
#    - Copia NEXT_PUBLIC_SANITY_PROJECT_ID

# ====================================
# VARIABLES CRÍTICAS (REQUERIDAS)
# ====================================
# Las siguientes variables DEBEN estar configuradas:
# - DATABASE_URL
# - AUTH_SECRET (NextAuth)
# - DRIVER_JWT_SECRET (API Conductores)

# ====================================
# SEGURIDAD
# ====================================
# IMPORTANTE:
# - NUNCA commitear .env.local al repositorio
# - Usar secrets diferentes para AUTH_SECRET y DRIVER_JWT_SECRET
# - En producción, configurar variables en tu proveedor de hosting (Cloudflare Pages, Vercel, etc.)
# - Rotar secrets regularmente
